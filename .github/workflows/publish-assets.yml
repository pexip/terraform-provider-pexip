name: Build and Publish Terraform Provider

on:
  workflow_dispatch:
    inputs:
      version:
        type: string
        description: Release version (v0.0.0)
        required: true
  workflow_call:
    inputs:
      version:
        type: string
        description: Release version (v0.0.0)
        required: true

permissions: {}

env:
  GO_VERSION: "1.24"
  GOPRIVATE: github.com/pexip
  PROVIDER_NAME: "pexip"

jobs:
  build:
    name: Build Assets
    permissions:
      contents: read

    strategy:
      matrix:
        goos: [linux, darwin, windows]
        goarch: [amd64, arm64]
        exclude:
          - goarch: arm64
            goos: windows

    uses: ./.github/workflows/reusable_build.yml
    with:
      version: ${{ inputs.version }}
      goos: ${{ matrix.goos }}
      goarch: ${{ matrix.goarch }}

  upload:
    name: Upload Assets
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write
    env:
      MANIFEST: "terraform-provider-${{ needs.build.outputs.provider_name }}_${{ needs.build.outputs.version }}_manifest.json"
      SHA256SUMS: "terraform-provider-${{ needs.build.outputs.provider_name }}_${{ needs.build.outputs.version }}_SHA256SUMS"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          path: build_artifacts

      - name: Flatten artifacts into ./dist
        shell: bash
        run: |
          mkdir -p dist
          for d in build_artifacts/*; do
            mv "$d"/*.zip dist/
          done
          ls -laR dist/

      - name: Generate manifest JSON
        shell: bash
        run: |
          make manifest VERSION=${{ inputs.version }} PROVIDER_NAME=${{ env.PROVIDER_NAME }}

      - name: Upload MANIFEST artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.MANIFEST }}
          path: "dist/${{ env.MANIFEST }}"
          retention-days: 7

      - name: Generate SHA256 checksum
        shell: bash
        run: |
          cd dist
          shasum -a 256 *.zip > terraform-provider-${{ env.PROVIDER_NAME }}_${{ needs.build.outputs.version }}_SHA256SUMS

      - name: Import GPG key
        shell: bash
        run: |
          echo "${{ secrets.GPG_PRIVATE_KEY }}" | gpg --batch --import
          echo "${{ secrets.GPG_PASSPHRASE }}" > passphrase.txt

      - name: Sign checksum file
        shell: bash
        run: |
          cd dist
          gpg --batch --yes --pinentry-mode loopback --passphrase-file ../passphrase.txt \
            --armor --output ${{ env.SHA256SUMS }}.sig \
            --detach-sign ${{ env.SHA256SUMS }}

      - name: Upload SHA256SUMS artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.SHA256SUMS }}
          path: "dist/${{ env.SHA256SUMS }}"
          retention-days: 7

      - name: Upload assets to release
        uses: softprops/action-gh-release@72f2c25fcb47643c292f7107632f7a47c1df5cd8 #v2.3.2
        with:
          tag_name: ${{ inputs.version }}
          files: |
            dist/*.zip
            dist/${{ env.SHA256SUMS }}
            dist/${{ env.SHA256SUMS }}.sig
            dist/${{ env.MANIFEST }}
          token: ${{ secrets.GITHUB_TOKEN }}